---
title: "v0.2.0-canary.1 Codex SDK refresh"
created-date: 2026-09-25
status: completed
agent: codex
---

## Scope

Advance the `v0.2.0-canary.1` Codex SDK baseline from `0.156.1` to `0.157.0`.

## Changes

- Set `@openai/codex-sdk` to `^0.157.0` in `package.json`; `bun.lock` resolves
  the matching Codex CLI and platform packages at `0.157.0`.
- Updated the baseline sentence in `README.md` and the
  [CLI integration guide](../../guides/cli-action-tool-integration-guide.md).
- Compared the published SDK packages: JavaScript and declarations are unchanged;
  the manifest advances the pinned Codex CLI runtime.

## Verification

- Frozen install, TypeScript check, lint, format check, build, Node.js SDK and
  package imports, CLI version checks, and `git diff --check` passed. The build
  retained the existing TypeScript 7 experimental-API warning.
- `bun run test:all` passed 1,405 unit, 2,147 application, 2 Codex, and 5 Pandoc
  cases with no failures or skips.
- `bun audit` found no known vulnerabilities across 271 packages; `bun outdated`
  reported no remaining candidates.
